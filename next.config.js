/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'basketball-analysis-rho.vercel.app'],
    unoptimized: true,
  },
}

module.exports = nextConfig

